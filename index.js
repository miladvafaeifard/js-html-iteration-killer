// Import stylesheets
import './style.css';

/* Query selections
  const ul = document.querySelector( "[data-bind=\"bar\"]" ),
  const lis = ul.getElementsByTagName( "li" );
 */
/* Don't do this! it will damage your browser :)
 for ( let i = 0; i < lis.length; i++ ) {
   const newLi = document.createElement( "li" );
   newLi.textContent = "Hello world";
   ul.appendChild(newLi);
 } 
 */

/* Add a new li
  setTimeout(function () {
    const newLi = document.createElement( "li" );
    newLi.textContent = "Hello world";
    ul.appendChild(newLi);
  }, 2000);
 */

